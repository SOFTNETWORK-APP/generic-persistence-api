syntax = "proto2";

import "scalapb/scalapb.proto";

package org.softnetwork.notification.model;

option (scalapb.options) = {
    single_file: true
    flat_package: true
};

enum Platform {
    UNKNOW_PLATFORM = 0;
    ANDROID = 1;
    IOS = 2;
    WEB = 3;
}
